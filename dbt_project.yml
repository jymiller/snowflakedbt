name: snowflakedbt
version: 1.0.0
config-version: 2
profile: snowflakedbt
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  staging:
    +materialized: view
  marts:
    +materialized: table
